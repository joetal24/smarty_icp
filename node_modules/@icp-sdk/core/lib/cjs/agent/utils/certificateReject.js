"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.readCertifiedReject = readCertifiedReject;
const certificate_ts_1 = require("../certificate.js");
const errors_ts_1 = require("../errors.js");
/**
 * Read certified reject details from a certificate at the given request status path.
 * @param certificate The certificate to read reject details from.
 * @param path The lookup path prefix (e.g. `['request_status', requestId]`).
 * @param requestId The request ID associated with the rejection.
 */
function readCertifiedReject(certificate, path, requestId) {
    const rejectCode = new Uint8Array((0, certificate_ts_1.lookupResultToBuffer)(certificate.lookup_path([...path, 'reject_code'])))[0];
    const rejectMessage = new TextDecoder().decode((0, certificate_ts_1.lookupResultToBuffer)(certificate.lookup_path([...path, 'reject_message'])));
    const errorCodeBuf = (0, certificate_ts_1.lookupResultToBuffer)(certificate.lookup_path([...path, 'error_code']));
    const errorCode = errorCodeBuf ? new TextDecoder().decode(errorCodeBuf) : undefined;
    return new errors_ts_1.CertifiedRejectErrorCode(requestId, rejectCode, rejectMessage, errorCode);
}
//# sourceMappingURL=certificateReject.js.map