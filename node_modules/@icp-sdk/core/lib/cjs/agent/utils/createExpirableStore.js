"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createExpirableStore = createExpirableStore;
const indexedDBExpirableStore_ts_1 = require("./indexedDBExpirableStore.js");
const inMemoryExpirableStore_ts_1 = require("./inMemoryExpirableStore.js");
/**
 * Creates an {@link ExpirableStore} with automatic runtime detection.
 * Uses {@link IndexedDBExpirableStore} in browser environments (when IndexedDB is available),
 * falls back to {@link InMemoryExpirableStore} otherwise.
 * @param options - Configuration for the store (dbName, storeName, and expirationTime).
 */
function createExpirableStore(options) {
    if (typeof globalThis.indexedDB !== 'undefined') {
        return new indexedDBExpirableStore_ts_1.IndexedDBExpirableStore(options);
    }
    return new inMemoryExpirableStore_ts_1.InMemoryExpirableStore({ expirationTime: options.expirationTime });
}
//# sourceMappingURL=createExpirableStore.js.map